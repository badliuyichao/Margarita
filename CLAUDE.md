# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目

Margarita — 一款个人物品盘点应用。核心理念是"少即是多"：清楚自己拥有什么，才知道真正需要什么。

采用 AGPL-3.0 许可证。

## 分支策略

- `main` — 生产 / 稳定分支
- `develop` — 活跃开发分支（新工作的默认分支）

## 当前状态

全新项目。目前仅有初始化提交（README、LICENSE）。技术栈、构建系统及代码尚未建立。

---

## 行为准则

以下准则旨在减少 LLM 编码中的常见错误。根据需要与项目特定说明合并使用。

**权衡：** 这些准则偏向谨慎而非速度。对于简单任务，可自行判断。

### 1. 先思考再编码

**不要假设。不要隐藏困惑。明确呈现权衡。**

实施之前：
- 明确陈述你的假设。如果不确定，直接提问。
- 如果存在多种解读，将它们全部呈现出来——不要默默选择。
- 如果有更简单的方案，说出来。在合理时提出异议。
- 如果有不清楚的地方，停下来。指出困惑所在，然后提问。

### 2. 简单优先

**用最少的代码解决问题。不做任何推测性设计。**

- 不添加需求之外的功能。
- 不为仅使用一次的代码创建抽象。
- 不做未被要求的"灵活性"或"可配置性"设计。
- 不为不可能发生的场景添加错误处理。
- 如果你写了 200 行代码而本可以精简到 50 行，那就重写它。

问自己："资深工程师会说这过度复杂吗？"如果答案是肯定的，就简化。

### 3. 精准修改

**只改必须改的。只清理你自己造成的混乱。**

编辑现有代码时：
- 不要"改善"相邻的代码、注释或格式。
- 不要重构没有坏的东西。
- 匹配现有的代码风格，即使你觉得有更好的写法。
- 如果注意到无关的废弃代码，提出来——但不要删除。

当你的修改产生了孤立的残留：
- 删除因你的改动而不再使用的导入、变量或函数。
- 除非被要求，否则不要删除之前就存在的废弃代码。

检验标准：每一行改动都应该能直接追溯到用户的请求。

### 4. 目标驱动执行

**定义成功标准。循环直至验证通过。**

将任务转化为可验证的目标：
- "添加验证" → "为无效输入编写测试，然后让测试通过"
- "修复 bug" → "编写一个能复现 bug 的测试，然后让它通过"
- "重构 X" → "确保重构前后测试均通过"

对于多步骤任务，给出简要计划：
```
1. [步骤] → 验证：[检查项]
2. [步骤] → 验证：[检查项]
3. [步骤] → 验证：[检查项]
```

明确清晰的成功标准让你能独立推进。模糊的标准（"把它做出来"）则需要不断追问澄清。

---

**这些准则有效的标志是：** diff 中不必要的改动更少，因过度复杂而重写的次数更少，澄清性问题在实施之前提出而非在犯错之后。
